# type: ignore

apply 'cxx'
apply 'run'
apply from_project='scripts'

compile_resources('images') {
  data_files = glob('resources/*.png')
}

cxx.compile('main') {
  depends_on 'scripts:images'
  sources = glob('src/*.c')
  pkg_config 'sdl2', 'sdl2_image'
}

run {
  depends_on 'main'
}
